[CLI]
# Project-wide render defaults. Override per-invocation on the command line.
media_dir = ./media
quality = medium_quality
# Opt into preview per-render with -p (e.g. `manim -pql ...`); off by default
# so headless/CI renders don't try to launch a video player.
preview = False
